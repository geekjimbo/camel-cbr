<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="              http://www.osgi.org/xmlns/blueprint/v1.0.0 https://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd              http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd">
    <camelContext id="cbr-example-context" xmlns="http://camel.apache.org/schema/blueprint">
        <route id="cbr-route">
            <from id="_from1" uri="file:work/cbr/input"/>
            <log id="_log1" message="Receiving order ${file:name}"/>
            <choice id="_choice1">
                <when id="_when1">
                    <jsonpath id="_xpath1">$.message[?(@.topic=='entity')]</jsonpath>
                    <log id="_log2" message="Sending order ${file:name} to the WS entity"/>
                    <setHeader headerName="CamelHttpMethod">
                      <constant>GET</constant>
                    </setHeader>
                    <to id="_to1" uri="jetty:http://localhost:3000/entity.json"/>
                </when>
                <when id="_when2">
                    <jsonpath id="_xpath2">$.message[?(@.topic=='data_workflow')]</jsonpath>
                    <log id="_log3" message="Sending order ${file:name} to WS data_workflow"/>
                    <setHeader headerName="CamelHttpMethod">
                      <constant>GET</constant>
                    </setHeader>
                    <to id="_to2" uri="jetty:http://localhost:4000/data_workflow.json"/>
                </when>
                <otherwise id="_otherwise1">
                    <log id="_log4" message="Sending order ${file:name} to otros folder"/>
                    <to id="_to3" uri="file:work/cbr/output/otros"/>
                </otherwise>
            </choice>
            <log id="_log5" message="Done processing ${file:name}"/>
        </route>
    </camelContext>
</blueprint>
